# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type User {
  id: ID!
  name: String!
  createdAt: String!
  age: Int!
  address: String!
}

type Query {
  users: [User!]!
}

input NewUser {
  name: String!
  age: Int!
  address: String!
}

type Mutation {
  createUser(input: NewUser!): User!
}
